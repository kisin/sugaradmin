<ul class="table_links">
	<% users.each do |user| %>
		<li>
			<%= link_to user.email, admin_user_path(user) %>
		</li>
	<% end %>
</ul>

<div class="table_action_items">
	<%= link_to "לחץ לצפייה בכל המשתמשים", admin_users_path %>
</div>